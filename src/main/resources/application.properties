# Database (existing)
spring.datasource.url=jdbc:mysql://localhost:3306/lankarail?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=thevindu2003

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# JWT settings (required)
jwt.secret=change_this_to_a_long_random_value_AtLeast32Chars1234
jwt.expirationMs=86400000

# Server port (optional)
server.port=8080
